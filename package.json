{
  "name": "yunfei-ma-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "check": "astro check",
    "test": "vitest run && bash tests/validate_agents_guidance.sh",
    "build": "astro build",
    "validate": "pnpm run check && pnpm run test && pnpm run build && node tests/validate-build.mjs"
  },
  "dependencies": {
    "astro": "7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
